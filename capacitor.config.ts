import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ishanmanjrekar.gamescaffold',
  appName: 'Game Scaffold',
  webDir: 'dist'
};

export default config;
